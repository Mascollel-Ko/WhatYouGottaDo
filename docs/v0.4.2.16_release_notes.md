# v0.4.2.16 Release Notes

v0.4.2.16 makes analysis chart dates understandable and fixes the main-exercise e1RM chart range without changing analysis calculations.

## Baseline

- Started from latest `origin/main` at `5eac6594b97504a9cacc87f478d7921e8fbad599`.
- Previous app identity: `v0.4.2.15 / 402015`.
- Shared temporal authority: `AnalysisChartTemporalPolicy`.

## Cause

- Daily and weekly analysis charts did not show enough date context.
- Weekly badminton volume and transfer charts could derive display labels independently.
- The fatigue card said `이번 주 누적 부담` even though it shows the selected rolling analysis period.
- The e1RM chart derived its horizontal range from the first displayed exercise series, so one bench-press observation could truncate longer squat and deadlift histories.

## Changes

- Weekly charts now use one Monday-Sunday authority. Thursday's month owns the week, and owned weeks are numbered chronologically within that month.
- Daily and weekly charts show readable X-axis labels, exact period subtitles and temporal accessibility descriptions.
- Label density affects labels only; source points remain intact.
- Badminton daily volume, weekly volume and weekly transfer stimulus now carry explicit `LocalDate` identity through the chart model.
- The rolling fatigue card is titled `누적 부담 흐름` and shows its actual start and end dates.
- Main-exercise e1RM uses the complete weekly union from the earliest through latest observation across all displayed exercises.
- Missing e1RM weeks remain absent: no zero fill, forward fill or undocumented interpolation.
- e1RM Y bounds use every finite visible value across all displayed exercises.
- Muscle-group and rep-range ratio trends use the same shared week labels.

## Preserved

- Badminton training-volume and transfer-stimulus calculations and colors.
- OFI calculations, recovery, five axes and state classification.
- Strength effective-volume and e1RM formulas, including the existing weekly maximum policy.
- Muscle mappings, rep-range definitions and ratio calculations.
- Connective-tissue analysis, ProgramBuilder, records and stable keys.
- Protocol library structure: 6 families and 28 canonical protocols.

## Verification

- Focused temporal, badminton, fatigue, e1RM, strength-series and chart presentation coverage: 65 tests, all passed.
- Full `:app:testDebugUnitTest`: 946 tests across 159 suites, zero failures, errors or skipped tests.
- `:app:compileDebugKotlin`: passed.
- `:app:compileDebugAndroidTestKotlin`: passed.
- `:app:assembleDebug`: passed.
- `:app:validateConnectiveTissuePriorBaselines`: passed; the separate tissue authority remains unchanged.
- Protocol documentation validation: passed for 6 families and the unchanged 28 protocols.
- `git diff --check`: passed for the implementation, tests and documentation.
- GitHub Actions is verified on the release commit before the annotated tag is created.

## Release Identity

- `versionName = "v0.4.2.16"`
- `versionCode = 402016`
