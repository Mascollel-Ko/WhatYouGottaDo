# v0.4.1.10.10 ProgramBuilder Search / Hard Include / Wide Pool Worklog

Baseline:
- Started from main at v0.4.1.10.9.
- Baseline commit: 8a63523dc3b7d720b1dd90e99eb893121bece339.
- Working branch: fix/program-builder-search-hard-include-wide-pool-v0411010.
- Push policy: user already pushed; no push from Codex for this task.

## Commit 1 - test(program-ui): reproduce include exclude exercise search gaps

Commit:
- 163fbe0

수정 원인:
- ProgramBuilder preferred/excluded exercise pickers reused a narrow helper that only matched exercise names and capped query results at six.
- This made it possible for valid active catalog exercises to be invisible in include/exclude search even before generation started.
- The gap is a UI-level candidate discovery problem, separate from ProgramBuilder generation scoring.

수정 골자:
- Added regression coverage for searching program include/exclude options by stable key, detail fields, equipment, and movement pattern.
- Added regression coverage that typed queries return all matching exercises instead of an arbitrary six-result slice.
- Production code was intentionally unchanged in this commit.

파일별 변경:
- `ProgramExerciseSelectionOptionsTest.kt`: adds search coverage for broader catalog fields and query-result count.

동작 변화:
- No production behavior change in this commit.
- The new tests currently expose the existing search gap.

다음 작업자 참고:
- Fix the picker helper rather than changing ProgramBuilder generation policy.
- Empty query may remain limited, but typed queries must show all matches.
- Metadata catalog aliases are not currently available in the UI helper input, so the immediate fix should cover the fields present on `Exercise`.

검증:
- `./gradlew :app:testDebugUnitTest --tests "*ProgramExerciseSelectionOptionsTest*"` failed as expected.
- Failing tests: `selectionOptionsSearchAcrossCatalogFieldsAndStableKey`, `selectionOptionsDoNotLimitSearchQueryResultsToSix`.

## Commit 2 - fix(program-ui): reuse full catalog exercise search for program include exclude pickers

Commit:
- 8cb7ab1

수정 원인:
- The include/exclude picker still treated search as a short name-only option list.
- Preferred hard include cannot work reliably if the user cannot find a valid exercise in the picker.

수정 골자:
- Broadened the program exercise picker helper to search the active catalog fields already available on `Exercise`.
- Kept the default empty-query option list limited.
- Removed the arbitrary six-result cap for typed queries.

파일별 변경:
- `PlanEditorControls.kt`: searches name, category, detail fields, stable key, movement fields, equipment, family, role, transfer, and muscle fields.
- `ProgramExerciseSelectionOptionsTest.kt`: the Commit 1 regression tests now pass.

동작 변화:
- Program include/exclude picker queries now surface all matching active exercises from the catalog fields.
- Empty query behavior remains compact.

다음 작업자 참고:
- Runtime metadata alias search still needs metadata to be provided to the UI helper; this commit deliberately avoids wiring new metadata dependencies into the picker.
- This commit does not change ProgramBuilder generation or preferred hard include behavior.

검증:
- `./gradlew :app:testDebugUnitTest --tests "*ProgramExerciseSelectionOptionsTest*"` passed.
- `./gradlew :app:compileDebugKotlin` passed.

## Commit 3 - test(program): require preferred exercises to survive generation

Commit:
- 03f93c4

수정 원인:
- Preferred exercises were treated as a small scoring boost instead of a placement guarantee.
- That allowed equipment, template, fatigue, time, slot, or session filtering to silently drop user-selected preferred exercises.
- Preferred/excluded stable-key conflicts also needed explicit HARD validation rather than silent exclusion.

수정 골자:
- Added a regression test requiring a preferred exercise to appear even when normal generation filters would reject it.
- Added a regression test requiring preferred/excluded conflicts to produce `PROGRAM_PREFERRED_EXCLUDED_CONFLICT` with HARD severity.
- Production code is intentionally unchanged in this commit.

파일별 변경:
- `ProgramBuilderPreferredHardIncludeV0411010Test.kt`: covers hard include survival and preferred/excluded conflict validation.

동작 변화:
- No production behavior change in this commit.
- The new tests expose preferred hard include gaps in the current builder.

다음 작업자 참고:
- Fix preferred handling through a direct active-exercise resolution path, not by increasing the existing preferred score boost.
- Missing, inactive, and excluded-conflict preferred keys are hard validation cases; equipment/time/fatigue/template mismatch should not silently drop preferred exercises.

검증:
- `./gradlew :app:testDebugUnitTest --tests "*ProgramBuilderPreferredHardIncludeV0411010Test*"` failed as expected.
- Failing tests: `preferredExerciseSurvivesEquipmentMismatchAndGenerationFilters`, `preferredExcludedConflictIsHardValidationIssue`.

## Commit 4 - fix(program): resolve preferred exercises through hard include path

Commit:
- 359b367

수정 원인:
- Preferred exercises needed a placement guarantee, not just a reranking boost.
- User-selected preferred keys could be dropped by equipment/template/fatigue/time/slot/session filters with only a generic warning.
- Preferred/excluded conflicts needed exact validation output.

수정 골자:
- Added a final preferred hard-include pass that resolves requested preferred stable keys directly from active exercises.
- Added HARD validation issues for missing, inactive, and preferred/excluded conflict keys.
- Places missing preferred exercises by replacing weak/non-preferred slots first, or appending only when no replacement target exists.
- Replaced the vague preferred-missing warning with exact code-based outcomes.

파일별 변경:
- `ProgramBuilder.kt`: adds preferred resolution, placement, validation, and exact outcome summary.
- `ProgramBuilderPreferredHardIncludeV0411010Test.kt`: Commit 3 regression tests now pass.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: updates summary assertions to code-based messages and verifies the generic warning is gone.

동작 변화:
- Preferred active exercises are included at least once in the generated skeleton unless they are missing, inactive, or conflict with excluded keys.
- Equipment/time/fatigue/template/session mismatches no longer silently drop preferred keys.
- Preferred/excluded conflicts surface as `PROGRAM_PREFERRED_EXCLUDED_CONFLICT` HARD validation.

다음 작업자 참고:
- This pass is intentionally placed after optimization so later repair cannot remove preferred exercises.
- Do not revert this back into a score-only boost.
- Future UI can translate the code-based messages, but generation should keep exact outcome codes.

검증:
- `./gradlew :app:testDebugUnitTest --tests "*ProgramBuilderPreferredHardIncludeV0411010Test*"` passed.
- `./gradlew :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` passed.
- `./gradlew :app:compileDebugKotlin` passed.

## Commit 5 - test(program): capture over restrictive candidate pool behavior

Commit:
- pending

수정 원인:
- Slot selection still allowed small candidate windows to survive from older top-k behavior.
- Small windows made it too easy for safe filler exercises to dominate before variety, preferred hard include, or beam scoring had enough alternatives.

수정 골자:
- Strengthened the slot-query regression test so a pool smaller than 30 does not truncate available candidates.
- Added direct coverage that variety sizing and beam candidate windows keep at least 30 candidates when enough scored candidates exist.
- Production code is intentionally unchanged in this commit.

파일별 변경:
- `ProgramBuilderCandidateCollapseTest.kt`: adds wide-pool expectations for slot query, variety policy, and beam window behavior.

동작 변화:
- No production behavior change in this commit.
- The new tests expose that current pool/beam constants remain too restrictive.

다음 작업자 참고:
- Fix candidate window sizing instead of loosening hard gates.
- The target is a wide scored pool, not random filler insertion.

검증:
- `./gradlew :app:testDebugUnitTest --tests "*ProgramBuilderCandidateCollapseTest*"` failed as expected.
- Failing tests: `slotQueryUsesAdaptivePoolInsteadOfFixedTopThree`, `slotSelectionPoliciesKeepThirtyCandidatesWhenAvailable`.
