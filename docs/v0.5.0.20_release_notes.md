# v0.5.0.20 release notes

## Scope

This is a test-portability follow-up to the Phase 2A.1 metadata release. The
new frozen strength-asset test compared platform-specific checkout bytes, so
it passed on Windows CRLF checkouts and failed on Linux LF checkouts.

## Change

- Normalize CRLF to LF before hashing the two unchanged production strength
  registry CSV files in `StrengthProxyPriorRegistryTest`.
- Preserve all Phase 2A.1 production behavior, metadata assets, prior values,
  Room, backup, analysis, ProgramBuilder, and UI behavior.
- Preserve the already-pushed `v0.5.0.19` tag without moving it.

## Verification

- Focused `StrengthProxyPriorRegistryTest`: passed locally.
- Full unit tests and debug APK assembly: verified by GitHub Actions before
  tagging `v0.5.0.20`.
