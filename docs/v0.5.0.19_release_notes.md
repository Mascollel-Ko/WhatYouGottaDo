# v0.5.0.19 release notes

## Release identity

- Baseline: `40615fab9c7ff892b0e48dd5a244eeb77e7cf2ee` (`0.5.0.18 / 500018`)
- Version name: `0.5.0.19`
- Version code: `500019`
- Release type: metadata strategy v2.3 Phase 2A.1 semantic correction and prior preflight

## Approved semantic corrections

- Restored the 26 approved legacy `trainingRole` assignments by exact stableKey only. Unlisted exercises remain empty, and the legacy roles are not promoted to final program-role taxonomy.
- Classified `familyId` as `DERIVED_NONCANONICAL` with target relation `NONE`.
- Classified `loadProfile` as `LEGACY_COMPOSITE_TO_BE_DECOMPOSED` with target relation `NONE`.
- Defined the target `sportTransferDirect` contract as a complete closed-world whitelist where absence is authoritative `NONE`.
- Preserved the current production name-based direct-transfer fallback unchanged because sport-transfer runtime cutover is outside this release.
- Kept persisted compatibility fields, Room, backup/restore, stableKeys, analysis calculations, and ProgramBuilder policy code unchanged.

## Isolated strength-proxy prior

- Added versioned typed assets for exactly five targets: `BENCH_PRESS`, `BACK_SQUAT`, generic `DEADLIFT`, `WEIGHTED_PULL_UP`, and `MILITARY_PRESS`.
- The product owner defines `ex_32219f7a` (`오버헤드 프레스`) as the `MILITARY_PRESS` direct anchor: standing strict barbell overhead press, no intentional knee or hip drive, and not push press or jerk. Existing records under the stableKey are treated as this canonical movement. Push/jerk movements require separate stableKeys.
- Seeded 24 exact stableKey/target relations. Non-direct rows remain broad `PROVISIONAL_PRODUCT_PRIOR / TEMPORARY_APPROVED` values requiring later research review.
- The new package and assets have no production strength-posterior consumer. Existing production target and proxy-loading assets remain byte-for-byte unchanged.

## Taxonomy preflight

- Added a decision matrix covering all 94 distinct current concepts represented by the 102 field/storage audit rows.
- Added an 86-row explicit Korean/English Level-1 reference-registry draft for movement, joint, event, phase, plane, laterality, and equipment concepts.
- Every registry row remains `DRAFT / REVIEW_REQUIRED`; no production display catalogue or stored value changed.

## Audit and frozen boundaries

- Corrected generated semantic audit artifacts and added inventories for training role, family, and load profile.
- The four Phase 0/1 production contract Kotlin files retain their baseline SHA-256 values.
- The six pre-existing modified `outputs/*` files were not changed, staged, or committed.

## Validation

- Focused metadata, prior-registry, architecture, frozen-analysis, and program timing/builder parity tests: passed.
- Deterministic metadata audit: passed with 102 field rows, 501 parsing/inference rows, 3,633 mapping rows, 20 risk paths, 4,480 stableKey impact rows, and 0 confirmed metadata errors.
- Protocol validation: passed for 8 families and 33 protocols.
- Full unit tests: 1,130 tests across 196 suites; zero failures, errors, or skips.
- `compileDebugKotlin`, `compileDebugAndroidTestKotlin`, and `assembleDebug`: passed.
- Debug APK: 47,055,663 bytes; SHA-256 `C6691066A1696579FF4186FE215A10371DCE19529EED89B9E3078D7D55D271F9`.

## Remaining risks and follow-up

- The Korean registry is a review draft, not approved `REVIEWED_V1` authority.
- Non-direct strength-proxy values need literature review after the metadata foundation is approved.
- Final purpose-specific program relations and removal of legacy compatibility fields remain Phase 2B or later work.
- A future `UserStrengthProxyPosterior` must live outside exercise metadata and avoid double-counting a proxy observation linked to multiple targets.
