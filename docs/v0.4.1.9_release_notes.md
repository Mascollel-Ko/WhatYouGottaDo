# v0.4.1.9 Daily-Timeseries Import Extraction Release Notes

v0.4.1.9 is a test-first daily-timeseries import extraction release.
It adds focused daily-timeseries import behavior regression coverage before moving importDailyTimeseriesCsv out of TrainingRepository.
It does not change daily-timeseries format, import semantics, backup restore behavior, metadata resolution, analysis, UI, Room schema, or ViewModel call sites.

## Korean Summary

v0.4.1.9는 daily-timeseries CSV import 동작을 테스트로 먼저 고정한 뒤 TrainingRepository에서 별도 service로 분리한 구조 개선 릴리스다.
daily-timeseries 포맷, import 동작, backup restore 동작, metadata resolution, 분석, UI, Room schema, ViewModel call site는 변경하지 않는다.
