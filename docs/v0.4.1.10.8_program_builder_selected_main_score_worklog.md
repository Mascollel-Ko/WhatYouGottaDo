# v0.4.1.10.8 ProgramBuilder Selected Main Score Worklog

Baseline:
- Started from latest main at `v0.4.1.10.7` (`5398433`).
- Work branch: `fix/program-builder-selected-main-score-v041108`.
- Existing unrelated `outputs/` and `.codex-remote-attachments/` changes were left unstaged.

## Commit 1 - test(program): reproduce selected main exercise scoring regression

Commit:
- pending

Cause:
- ProgramBuilder still allowed safe filler/core choices to survive final selection without an explicit exact-identity score correction for the requested main exercises.
- Captain chair leg raise repetition needed a narrow regression test so the fix could avoid penalizing all core or leg-raise exercises.

Change Summary:
- Added tests for the exact selected-main 1.40 final score multiplier.
- Added broad-category guard tests so RDL, rows, bench/incline/landmine press, leg press, split/goblet/front/box squat, trap-bar deadlift, and core/prehab options do not receive the boost.
- Added tests for a captain-chair-only 0.70 multiplier while leaving hanging leg raise, dead bug, Pallof press, side plank, and crunch untouched.

Files:
- `app/src/test/java/com/training/trackplanner/data/ProgramSelectedExerciseScorePolicyTest.kt`: selected-main boost, broad category non-boost, and captain-chair-only penalty regression tests.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: worklog.

Behavior:
- Test-only commit. Production behavior is intentionally unchanged here.

Next Worker Notes:
- The tests intentionally fail until `ProgramSelectedExerciseScorePolicy` is implemented and wired into final score selection.
- Keep the boost exact-identity based; do not turn it into a movement-family boost.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramSelectedExerciseScorePolicyTest*"` failed as expected with `Unresolved reference 'ProgramSelectedExerciseScorePolicy'`.
