# v0.4.1.10.8 ProgramBuilder Selected Main Score Worklog

Baseline:
- Started from latest main at `v0.4.1.10.7` (`5398433`).
- Work branch: `fix/program-builder-selected-main-score-v041108`.
- Existing unrelated `outputs/` and `.codex-remote-attachments/` changes were left unstaged.

## Commit 1 - test(program): reproduce selected main exercise scoring regression

Commit:
- `e79e5bf`

Cause:
- ProgramBuilder still allowed safe filler/core choices to survive final selection without an explicit exact-identity score correction for the requested main exercises.
- Captain chair leg raise repetition needed a narrow regression test so the fix could avoid penalizing all core or leg-raise exercises.

Change Summary:
- Added tests for the exact selected-main 1.40 final score multiplier.
- Added broad-category guard tests so RDL, rows, bench/incline/landmine press, leg press, split/goblet/front/box squat, trap-bar deadlift, and core/prehab options do not receive the boost.
- Added tests for a captain-chair-only 0.70 multiplier while leaving hanging leg raise, dead bug, Pallof press, side plank, and crunch untouched.

Files:
- `app/src/test/java/com/training/trackplanner/data/ProgramSelectedExerciseScorePolicyTest.kt`: selected-main boost, broad category non-boost, and captain-chair-only penalty regression tests.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: worklog.

Behavior:
- Test-only commit. Production behavior is intentionally unchanged here.

Next Worker Notes:
- The tests intentionally fail until `ProgramSelectedExerciseScorePolicy` is implemented and wired into final score selection.
- Keep the boost exact-identity based; do not turn it into a movement-family boost.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramSelectedExerciseScorePolicyTest*"` failed as expected with `Unresolved reference 'ProgramSelectedExerciseScorePolicy'`.

## Commit 2 - fix(program): boost selected main exercises by forty percent

Commit:
- `a08a181`

Cause:
- The selected main exercises needed an explicit final-score multiplier after hard gates and context reranking.
- Broad foundation categories were already handled by existing scoring/reranking policy, so this change had to stay exact-identity based.

Change Summary:
- Added `ProgramSelectedExerciseScorePolicy`.
- Applied a 1.40 multiplier only to the exact selected main exercise allowlist.
- Wired ProgramBuilder final score calculation through the policy after base scoring and context reranking.
- Left captain chair penalty for the next isolated commit.

Files:
- `app/src/main/java/com/training/trackplanner/data/ProgramSelectedExerciseScorePolicy.kt`: exact stableKey/name allowlist and selected-main multiplier.
- `app/src/main/java/com/training/trackplanner/data/ProgramBuilder.kt`: applies selected-main score adjustment after context reranking.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: commit notes.

Behavior:
- `barbell_back_squat`, `barbell_deadlift`, `pull_up`, `ex_32219f7a`, and `ex_8e1b313e` receive a 1.40 final score multiplier.
- RDL, rows, bench/incline/landmine press, leg press, split/goblet/front/box squat, trap-bar deadlift, and broad core/prehab examples are not boosted.
- Excluded, safety, and unavailable-equipment candidates remain controlled by existing hard gates before scoring.

Next Worker Notes:
- Do not widen `SELECTED_MAIN_STABLE_KEYS` into movement-family matching.
- Captain chair penalty is intentionally not implemented in this commit.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramSelectedExerciseScorePolicyTest*"` compiled and failed only on the pending captain-chair penalty assertion.
- `.\gradlew.bat :app:compileDebugKotlin` ran as part of the focused test task and passed.

## Commit 3 - fix(program): penalize captain chair leg raise by thirty percent

Commit:
- `c309cf1`

Cause:
- Captain chair leg raise was still not receiving the requested narrow final-score penalty after the selected-main boost was wired.
- The penalty had to avoid broad leg-raise, trunk-flexion, and general core categories.

Change Summary:
- Added a 0.70 multiplier for exact captain chair leg raise identities only.
- Kept hanging leg raise, dead bug, Pallof press, side plank, crunch, and other core exercises unpenalized.
- Applied the captain-chair multiplier in the same final-score policy as the selected-main boost.

Files:
- `app/src/main/java/com/training/trackplanner/data/ProgramSelectedExerciseScorePolicy.kt`: captain-chair-only exact identity penalty.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: commit notes.

Behavior:
- `ex_a345e30b` and legacy test key `captain_chair_leg_raise` receive a 0.70 final score multiplier.
- Other leg raise or core examples do not receive the captain-chair penalty.
- Preferred score effects still occur before this final multiplier, so preferred selection does not completely erase the captain-chair penalty.

Next Worker Notes:
- Keep captain chair matching exact; do not replace it with all leg raises or all trunk flexion.
- This policy still runs after hard gates, so exclusion/safety/equipment behavior stays upstream.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramSelectedExerciseScorePolicyTest*"` passed.
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilder*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` was covered by the focused test tasks and passed/up-to-date.

## Commit 4 - fix(program): trace selected exercise score adjustments

Commit:
- `5e8e7d1`

Cause:
- If final recommendations stay unchanged after score multipliers, the next likely failure point is candidate exclusion before scoring.
- Existing traces showed stage counts but did not expose per-candidate base score, context rerank, final score, or selected-main/captain-chair adjustment flags.

Change Summary:
- Added per-candidate score adjustment trace rows to `ProgramCandidateTrace`.
- Recorded exercise name, stableKey, base score, context rerank score, selected-main boost flag, captain-chair penalty flag, final score, selection-window inclusion, and final selected status.
- Kept the trace internal to generated skeleton/debug/test surfaces; no normal UI score table was added.

Files:
- `app/src/main/java/com/training/trackplanner/data/ProgramSkeletonGenerator.kt`: score trace model fields.
- `app/src/main/java/com/training/trackplanner/data/ProgramSlotCandidateQuery.kt`: attaches score traces and marks selection-window inclusion.
- `app/src/main/java/com/training/trackplanner/data/ProgramBuilder.kt`: records base/rerank/final score details and marks the selected candidate.
- `app/src/test/java/com/training/trackplanner/data/ProgramBuilderCandidateCollapseTest.kt`: verifies score trace details are present.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: commit notes.

Behavior:
- Program generation behavior is unchanged except for richer internal trace data.
- Debug/test consumers can now tell whether a candidate reached scoring, entered the selection window, and was finally selected.

Next Worker Notes:
- If selected-main outputs still do not change, inspect `scoreAdjustments` for empty rows or candidates missing before `sessionAllowed`.
- Do not expose this as a normal user-facing score table without a separate UI decision.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilder*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 5 - chore(release): bump version to v0.4.1.10.8

Commit:
- pending at commit time

Cause:
- The selected-main score policy, captain-chair penalty, and score trace changes need a release identity.

Change Summary:
- Bumped app version metadata to `v0.4.1.10.8` / `401018`.
- Added v0.4.1.10.8 release notes.
- Finalized worklog commit hashes for implementation commits.

Files:
- `app/build.gradle.kts`: versionName/versionCode bump.
- `docs/v0.4.1.10.8_release_notes.md`: release notes.
- `docs/v0.4.1.10.8_program_builder_selected_main_score_worklog.md`: final worklog.

Behavior:
- App build identity is v0.4.1.10.8.
- No additional ProgramBuilder scoring behavior changes in this release commit.

Next Worker Notes:
- If generated results remain unchanged, inspect `candidateTraces.scoreAdjustments` before changing multipliers.
- Existing unrelated `outputs/` files remain intentionally unstaged.

Tests:
- Final `.\gradlew.bat :app:testDebugUnitTest` passed.
- Final `.\gradlew.bat :app:assembleDebug` passed.
