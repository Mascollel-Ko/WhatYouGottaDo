# v0.4.2.14 Release Notes

v0.4.2.14 is a small connective-tissue presentation and restore compatibility hotfix.

## Baseline

- Started from latest `origin/main` commit `934c6cc85b3e9f8e3d807780687d4a329261ece7`.
- Previous app identity: `v0.4.2.13 / 402013`.
- Final app identity: `v0.4.2.14 / 402014`.

## Changes

- Removed domain diagnostics from `TissueAnalysisUiState`, child presentation rows and Compose rendering. Developer messages, raw stable keys and diagnostic lists remain available to domain logs and tests but are not user-facing.
- Kept the legitimate baseline-source footer unchanged and visible below the analysis content.
- Added one exact restore-boundary normalization from legacy `imported_배드민턴` to canonical `ex_ae9ecdbc` for exercise rows, record rows and restored runtime metadata.
- Reused the existing reviewed badminton connective-tissue authority without adding a mapping or runtime display-name fallback.
- When event counts are absent, restored badminton sessions retain the existing duration/RPE exposure path. No jump, lunge, deceleration or direction-change count is fabricated.

## Behavior Preserved

- Badminton tissue mapping values and all connective-tissue exposure, recovery, prior, PersonalBaseline, `w_perUnit`, classification and ranking calculations are unchanged.
- Educational descriptions, OFI, ProgramBuilder and all other exercise stable keys are unchanged.

## Verification

- Focused `BackupRestoreImportBehaviorTest` and `ConnectiveTissueAnalysisUiTest`: 18 tests passed with 0 failures.
- The import regression uses the production Room restore path and production tissue assets, verifies the complete canonical badminton mapping-key set, 45-minute duration dose and RPE 8 effort, and verifies zero recorded repetitions remain zero.
- Full `:app:testDebugUnitTest`: 924 tests across 156 suites passed with 0 failures, errors or skipped tests.
- `:app:compileDebugKotlin` and `:app:assembleDebug`: passed.
- `validateConnectiveTissuePriorBaselines`: passed; connective-tissue numerical authorities remain unchanged.
- Protocol documentation validation: passed for 6 families and the unchanged 28 protocols.

## Known Boundary

- This normalization applies at backup restore. It intentionally does not add runtime name matching inside tissue calculation.
