# v0.4.1.10.6 Program Builder Fatigue and Selection Worklog

## Commit 1 - test(program): reproduce fatigue and exercise preference gaps

수정 원인:
- v0.4.1.10.5 이후에도 planning fatigue gate가 오늘 실행용 hard gate처럼 foundation anchor를 후보 단계에서 제거할 수 있다.
- ProgramSkeletonRequest에는 실제 선택된 제외/우선 운동 stableKey가 없고 free-text memo만 전달된다.

수정 골자:
- v0.4.1.10.6 전용 회귀 테스트 파일을 추가했다.
- 현재 gap을 재현하는 통과 가능한 baseline 테스트로 다음 production 변경의 기준점을 만든다.

파일별 변경:
- `app/src/test/java/com/training/trackplanner/data/ProgramBuilderFatigueAndSelectionV041106Test.kt`: fatigue planning gate와 exercise preference request gap 재현 테스트.
- `docs/v0.4.1.10.6_program_builder_fatigue_and_selection_worklog.md`: 커밋별 작업 로그 시작.

동작 변화:
- production 동작 변경 없음.

다음 작업자 참고:
- 다음 커밋부터 이 테스트는 새 expected behavior에 맞춰 갱신된다.
- 오늘 실행 gate와 미래 planning gate를 분리하되, 오늘 적용 경로의 hard safety behavior는 유지해야 한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` 통과.

## Commit 6 - fix(program): apply selected exercise preferences during candidate selection

수정 원인:
- request가 stableKey selections를 운반해도 builder가 이를 사용하지 않으면 UI 선택이 생성 결과에 영향을 주지 않는다.
- free-text memo를 hard exclude로 쓰면 실제 선택 없이 이름 포함만으로 후보가 사라질 수 있다.

수정 골자:
- `ProgramCandidateInventory`가 selected excluded stableKey만 hard exclude하도록 변경했다.
- ProgramBuilder가 request의 excluded stableKey set을 inventory에 전달한다.
- `ProgramCandidateRerankingPolicy`가 preferred stableKey 후보에 boost를 준다.
- free-text memo는 hard gate에서 제외했다.

파일별 변경:
- `ProgramCandidateInventory.kt`: stableKey hard exclude 적용.
- `ProgramBuilder.kt`: selected excluded stableKey 전달.
- `ProgramCandidateRerankingPolicy.kt`: preferred stableKey score boost.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: hard exclude/boost 검증.
- `ProgramBuilderV0352Test.kt`: 새 fatigue planning 정책에 맞게 high impact 제거와 foundation downscale expectation 갱신.

동작 변화:
- 실제 선택한 제외 운동만 후보에서 제거된다.
- 실제 선택한 우선 운동은 hard force가 아니라 점수 boost를 받는다.
- legacy memo는 기록/메모 역할로 남고 후보 hard gate로 쓰이지 않는다.

다음 작업자 참고:
- preferred boost는 안전/장비/slot gate를 무시하지 않는다.
- selected excluded stableKey는 hard gate이므로 UI에서 실수 선택을 쉽게 해제할 수 있어야 한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*ProgramBuilder*"` 통과.

## Commit 7 - feat(program): select avoided and preferred exercises from search

수정 원인:
- free-text memo만으로는 사용자가 실제 어떤 운동을 제외/우선하려는지 builder가 안전하게 알 수 없다.
- stableKey 기반 request fields를 채우려면 생성 화면에서 실제 운동 목록 검색 선택이 필요하다.

수정 골자:
- program editor 자동 골자 옵션 영역에 제외할 운동/우선 포함할 운동 검색 선택 UI를 추가했다.
- 선택된 운동은 stableKey list로 저장되어 `currentRequest()`에 전달된다.
- 검색 옵션 helper는 active exercise만 보여주고 이미 선택된 운동은 제외한다.

파일별 변경:
- `PlanScreen.kt`: ExerciseStableKeyPicker UI 추가.
- `PlanEditorControls.kt`: `programExerciseSelectionOptions` helper 추가.
- `ProgramExerciseSelectionOptionsTest.kt`: 검색/선택 filtering 검증.

동작 변화:
- 사용자는 자동 골자 생성 전에 실제 운동을 검색해 제외 또는 우선 포함 대상으로 선택할 수 있다.
- 저장 schema는 변경하지 않았고, 선택은 생성 request에만 반영된다.

다음 작업자 참고:
- 선택 UI는 최소 구현이다. 대량 운동 목록 성능 문제가 생기면 paging/search component로 교체하면 된다.
- 제외와 우선 포함에 같은 운동을 동시에 넣는 방지는 아직 UI에서 강제하지 않는다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramExerciseSelectionOptionsTest*"` 통과. 이 과정에서 `:app:compileDebugKotlin`도 통과.

## Commit 8 - fix(program): summarize selected exercise constraints after optimization

수정 원인:
- 사용자가 선택한 제외/우선 운동이 생성 과정에 반영됐는지 완료 후 짧게 확인할 수 있어야 한다.
- 상세 점수표를 노출하지 않고도 선택 제약 적용 결과는 요약으로 알려줄 수 있다.

수정 골자:
- ProgramBuilder optimization 이후 selected exercise constraint summary를 warnings와 optimization summary에 덧붙인다.
- 제외 운동 개수, 반영된 우선 포함 운동 개수, 반영되지 못한 우선 포함 운동을 짧게 안내한다.

파일별 변경:
- `ProgramBuilder.kt`: `withExerciseConstraintSummary` helper 추가 및 builder 반환 경로에 연결.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: constraint summary 검증.

동작 변화:
- 자동 골자 완료 후 summary에 선택 제약 반영 결과가 표시된다.
- 선택/평가/repair 알고리즘 자체는 변경하지 않았다.

다음 작업자 참고:
- summary는 사용자-facing 짧은 문구다. 상세 debug trace를 여기에 넣지 말 것.
- preferred missing은 hard failure가 아니라 조건 불일치 안내다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` 통과.

## Commit 5 - feat(program): carry selected exercise preferences into builder requests

수정 원인:
- 제외/우선 운동이 free-text memo에만 있으면 ProgramBuilder가 실제 운동 id/stableKey를 알 수 없다.
- 검색 UI와 candidate selection policy를 연결하려면 request가 stableKey set을 운반해야 한다.

수정 골자:
- `ProgramSkeletonRequest`에 `excludedExerciseStableKeys`, `preferredExerciseStableKeys` 기본 empty set 필드를 추가했다.
- Program editor state와 `currentRequest()`가 해당 stableKey list를 request로 전달하도록 준비했다.
- 기존 `excludedExerciseText` memo 필드는 유지했다.

파일별 변경:
- `ProgramSkeletonGenerator.kt`: request stableKey fields 추가.
- `PlanScreen.kt`: editor-local selected stableKey state와 request 전달 추가.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: request field/default 검증.

동작 변화:
- 아직 UI 선택이나 candidate 적용은 없다.
- 새 request fields는 기본 empty set이라 기존 호출부 동작은 유지된다.

다음 작업자 참고:
- DB schema에 저장 필드를 추가하지 않았다. 현재 선택은 자동 골자 생성 화면의 transient request state다.
- 실제 hard exclude / preferred boost는 다음 커밋에서 builder policy에 연결한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` 통과.

## Commit 2 - fix(program): separate planning fatigue from today execution gates

수정 원인:
- ProgramBuilder가 multi-week planning에서도 오늘 실행용 fatigue hard gate를 사용하면 foundation anchor 후보가 사라질 수 있다.
- 오늘 실제 적용 경로의 hard safety gate와 미래 계획 생성의 조정 gate는 의미가 다르다.

수정 골자:
- `ProgramFatigueUseCase`를 추가해 `PROGRAM_PLANNING`과 `TODAY_EXECUTION`을 구분했다.
- `FatigueSlotPolicy.allows`의 기본값은 today execution으로 유지하고, ProgramBuilder만 planning 모드를 명시한다.
- planning 모드는 `LOW_LOAD_ONLY` 후보를 제거하지 않고 이후 처방 축소 대상으로 남긴다.

파일별 변경:
- `FatigueSlotPolicy.kt`: fatigue use-case enum과 planning allow path 추가.
- `ProgramBuilder.kt`: schedule adaptation과 slot fatigue filter에서 planning use-case 사용.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: today execution hard block 유지와 planning 후보 유지 검증.

동작 변화:
- 미래 프로그램 생성은 fatigue 때문에 foundation anchor를 후보 단계에서 제거하지 않는다.
- 오늘 프로그램 적용 경로의 hard block/downgrade 동작은 유지된다.

다음 작업자 참고:
- planning mode가 후보를 남기더라도 처방은 다음 커밋에서 volume/RPE를 낮춰야 한다.
- execution default를 바꾸면 `ProgramPlanService` 적용 안전 동작이 바뀔 수 있으므로 유지해야 한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*FatigueSlotPolicyTest*"` 통과.

## Commit 3 - fix(program): downscale foundation anchors instead of filtering them

수정 원인:
- planning mode가 foundation anchor 후보를 남겨도 처방과 중량 강도가 그대로면 fatigue 조정 목적이 약해진다.
- 사용자가 원하는 동작은 anchor 제거가 아니라 set/RPE/weight intensity 축소다.

수정 골자:
- heavy lower foundation anchor가 ORANGE/RED planning fatigue에서 2세트 이하로 제한되도록 했다.
- RED heavy lower anchor의 planned RPE cap을 6으로 낮췄다.
- ProgramBuilder weight suggestion intensity multiplier에 planning fatigue load factor를 곱했다.

파일별 변경:
- `ProgramPrescriptionPolicy.kt`: foundation anchor fatigue downscale 규칙 추가.
- `ProgramBuilder.kt`: fatigue-aware weight suggestion intensity 적용.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: RED fatigue foundation anchor 처방 축소 검증.

동작 변화:
- future planning에서 anchor 후보는 유지되지만 RED/ORANGE fatigue에서는 작은 dose로 생성된다.
- today execution hard safety gate는 변경하지 않았다.

다음 작업자 참고:
- fatigue downscale 값은 보수적 heuristic이다. 데이터 기반 튜닝이 필요하면 `ProgramPrescriptionPolicy`에서 조정한다.
- 이 커밋은 warning 문구를 바꾸지 않는다. 문구 완화는 다음 커밋에서 처리한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*ProgramPrescriptionPolicyBehaviorTest*"` 통과.

## Commit 4 - fix(program): soften fatigue wording for planning adjustments

수정 원인:
- planning fatigue warning/reason이 사용자에게 운동 금지처럼 읽히면 실제 동작인 "강도 조정"과 맞지 않는다.
- v0.4.1.10.6의 핵심은 foundation anchor 삭제가 아니라 계획상 강도 축소다.

수정 골자:
- ProgramBuilder fatigue warning을 "계획 피로도 참고" 문구로 바꿨다.
- selection reason도 `계획피로조정`으로 표시해 planning adjustment임을 명확히 했다.

파일별 변경:
- `ProgramBuilder.kt`: fatigue warning wording 변경.
- `ProgramSelectionReasonFormatter.kt`: fatigue reason wording 변경.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: planning fatigue reason wording 검증.

동작 변화:
- 생성 결과의 안내 문구가 hard block 대신 planning adjustment 의미를 전달한다.
- 후보 선택/처방 로직은 변경하지 않았다.

다음 작업자 참고:
- 오늘 실행 gate 관련 문구와 혼동하지 말 것.
- 상세 fatigue 점수표를 UI에 노출하는 작업은 이번 범위가 아니다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` 통과.
