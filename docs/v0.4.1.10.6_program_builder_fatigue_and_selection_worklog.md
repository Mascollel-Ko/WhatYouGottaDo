# v0.4.1.10.6 Program Builder Fatigue and Selection Worklog

## Commit 1 - test(program): reproduce fatigue and exercise preference gaps

Cause:
- v0.4.1.10.5 still had a risk that planning fatigue could behave like a today execution hard gate.
- ProgramSkeletonRequest carried only a free-text exercise memo and no selected exercise stable keys.

Change Summary:
- Added focused v0.4.1.10.6 regression tests for the fatigue and exercise preference gaps.
- Started this release worklog.

Files:
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: baseline gap coverage.
- `v0.4.1.10.6_program_builder_fatigue_and_selection_worklog.md`: worklog.

Behavior:
- No production behavior change.

Next Worker Notes:
- Follow-up commits update these tests to the new expected behavior.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` passed.

## Commit 2 - fix(program): separate planning fatigue from today execution gates

Cause:
- Future program planning and today execution safety had different semantics but shared one gate behavior.

Change Summary:
- Added `ProgramFatigueUseCase`.
- Kept `TODAY_EXECUTION` as the default.
- Updated ProgramBuilder to use `PROGRAM_PLANNING` for schedule adaptation and fatigue candidate filtering.

Files:
- `FatigueSlotPolicy.kt`: use-case split.
- `ProgramBuilder.kt`: planning-mode calls.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: execution-vs-planning coverage.

Behavior:
- Planning keeps low-load foundation candidates available.
- Today execution hard block/downgrade behavior remains intact.

Next Worker Notes:
- Do not change the default `allows()` mode without checking ProgramPlanService today application behavior.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*FatigueSlotPolicyTest*"` passed.

## Commit 3 - fix(program): downscale foundation anchors instead of filtering them

Cause:
- Keeping foundation anchors under planning fatigue is only safe if their dose is reduced.

Change Summary:
- Limited heavy lower foundation anchor sets under ORANGE/RED fatigue.
- Lowered RED heavy lower RPE cap.
- Applied a fatigue load factor to weight suggestion intensity.

Files:
- `ProgramPrescriptionPolicy.kt`: set/RPE downscale.
- `ProgramBuilder.kt`: fatigue-aware weight suggestion multiplier.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: RED downscale coverage.

Behavior:
- Planning keeps foundation anchors but generates smaller, lower-RPE doses.

Next Worker Notes:
- Tune downscale values in `ProgramPrescriptionPolicy` if real data warrants it.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*ProgramPrescriptionPolicyBehaviorTest*"` passed.

## Commit 4 - fix(program): soften fatigue wording for planning adjustments

Cause:
- Planning fatigue messages could read like hard bans instead of planned dose adjustments.

Change Summary:
- Reworded generated warnings and selection reasons to use planning-adjustment language.

Files:
- `ProgramBuilder.kt`: warning text.
- `ProgramSelectionReasonFormatter.kt`: reason text.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: wording coverage.

Behavior:
- User-facing wording now reflects planning adjustment semantics.

Next Worker Notes:
- Keep today execution safety messaging separate from planning adjustment messaging.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` passed.

## Commit 5 - feat(program): carry selected exercise preferences into builder requests

Cause:
- Builder requests needed real selected exercise stable keys before UI and candidate policy could use them.

Change Summary:
- Added excluded/preferred stableKey sets to `ProgramSkeletonRequest`.
- Added editor-local state and included it in `currentRequest()`.
- Kept `excludedExerciseText` as a legacy memo field.

Files:
- `ProgramSkeletonGenerator.kt`: request fields.
- `PlanScreen.kt`: request state wiring.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: request-shape coverage.

Behavior:
- Existing callers default to empty selections.
- No Room schema change.

Next Worker Notes:
- StableKey selections are transient generation request inputs.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` passed.

## Commit 6 - fix(program): apply selected exercise preferences during candidate selection

Cause:
- StableKey selections were carried by the request but not yet applied to candidate selection.

Change Summary:
- Applied excluded stableKeys as hard candidate excludes.
- Applied preferred stableKeys as reranking boosts.
- Stopped using free-text memo as the primary hard exclude path.
- Updated the older red fatigue builder test to match the new planning policy.

Files:
- `ProgramCandidateInventory.kt`: stableKey hard exclude.
- `ProgramBuilder.kt`: passes selected excludes.
- `ProgramCandidateRerankingPolicy.kt`: preferred boost.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: exclude/boost tests.
- `ProgramBuilderV0352Test.kt`: fatigue expectation update.

Behavior:
- Selected avoided exercises are removed from candidates.
- Selected preferred exercises are boosted but not forced through invalid gates.

Next Worker Notes:
- Preferred exercises are soft preferences, not hard requirements.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*" --tests "*ProgramBuilder*"` passed.

## Commit 7 - feat(program): select avoided and preferred exercises from search

Cause:
- Users needed a real exercise search selection UI instead of memo-only input.

Change Summary:
- Added avoided/preferred exercise search pickers to the auto-skeleton options.
- Added a helper to filter active, unselected exercise options by name.

Files:
- `PlanScreen.kt`: search picker UI.
- `PlanEditorControls.kt`: option filtering helper.
- `ProgramExerciseSelectionOptionsTest.kt`: helper test.

Behavior:
- Users can select real exercises to avoid or prefer before generating a skeleton.
- Selections are request-only and not persisted to saved program schema.

Next Worker Notes:
- Prevent selecting the same exercise as both avoided and preferred if this becomes confusing.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramExerciseSelectionOptionsTest*"` passed; `:app:compileDebugKotlin` also passed.

## Commit 8 - fix(program): summarize selected exercise constraints after optimization

Cause:
- Users needed short feedback on whether selected exercise constraints were reflected.

Change Summary:
- Added post-optimization summary messages for excluded and preferred exercise selections.

Files:
- `ProgramBuilder.kt`: `withExerciseConstraintSummary`.
- `ProgramBuilderFatigueAndSelectionV041106Test.kt`: summary coverage.

Behavior:
- Completion summary now reports selected exercise constraint results.

Next Worker Notes:
- Keep this as short user-facing text, not debug trace output.

Tests:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` passed.

## Commit 9 - chore(release): bump version to v0.4.1.10.6

Cause:
- The fatigue planning and exercise selection improvements needed release metadata and final verification.

Change Summary:
- Bumped app version metadata to `v0.4.1.10.6` / `401016`.
- Added release notes.
- Recorded final verification.
- Matched final full-test safety expectation by keeping explosive transfer work out of ORANGE/RED planning fatigue while preserving downscaled foundation anchors.

Files:
- `FatigueSlotPolicy.kt`: planning fatigue keeps foundation anchors but excludes explosive transfer work.
- `app/build.gradle.kts`: version bump.
- `docs/v0.4.1.10.6_release_notes.md`: release notes.
- `docs/v0.4.1.10.6_program_builder_fatigue_and_selection_worklog.md`: final worklog.

Behavior:
- App build identity is v0.4.1.10.6.
- Planning fatigue keeps foundation work as low dose but excludes explosive transfer work when fatigue is ORANGE/RED.

Next Worker Notes:
- Start the next ProgramBuilder task from v0.4.1.10.6 on latest main.
- Persisted avoid/prefer stableKey constraints remain a follow-up candidate.

Tests:
- `.\gradlew.bat --version` passed.
- `.\gradlew.bat :app:testDebugUnitTest` passed.
- `.\gradlew.bat :app:assembleDebug` passed.
