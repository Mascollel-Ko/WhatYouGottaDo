# v0.4.1.10.6 Program Builder Fatigue and Selection Worklog

## Commit 1 - test(program): reproduce fatigue and exercise preference gaps

수정 원인:
- v0.4.1.10.5 이후에도 planning fatigue gate가 오늘 실행용 hard gate처럼 foundation anchor를 후보 단계에서 제거할 수 있다.
- ProgramSkeletonRequest에는 실제 선택된 제외/우선 운동 stableKey가 없고 free-text memo만 전달된다.

수정 골자:
- v0.4.1.10.6 전용 회귀 테스트 파일을 추가했다.
- 현재 gap을 재현하는 통과 가능한 baseline 테스트로 다음 production 변경의 기준점을 만든다.

파일별 변경:
- `app/src/test/java/com/training/trackplanner/data/ProgramBuilderFatigueAndSelectionV041106Test.kt`: fatigue planning gate와 exercise preference request gap 재현 테스트.
- `docs/v0.4.1.10.6_program_builder_fatigue_and_selection_worklog.md`: 커밋별 작업 로그 시작.

동작 변화:
- production 동작 변경 없음.

다음 작업자 참고:
- 다음 커밋부터 이 테스트는 새 expected behavior에 맞춰 갱신된다.
- 오늘 실행 gate와 미래 planning gate를 분리하되, 오늘 적용 경로의 hard safety behavior는 유지해야 한다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderFatigueAndSelectionV041106Test*"` 통과.
