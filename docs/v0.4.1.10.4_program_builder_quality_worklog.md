# v0.4.1.10.4 ProgramBuilder Quality Worklog

Baseline:
- Started from `v0.4.1.10.3` on `main`.
- Baseline commit: `a18ca53d76413a9b10d53b206f36abaa5f2d65be`.
- Reproduction: badminton support weight plan, 4 weeks, 5 days/week, 45 minutes/day, 60:40 badminton:strength, all equipment selected.

## Commit 1 — test(program): reproduce builder recommendation quality gaps

수정 원인:
- v0.4.1.10.3는 ProgramBuilder 구조를 나눴지만, 실제 자동 골자 품질 회귀 조건은 별도 테스트로 고정되어 있지 않았다.
- 전 장비 선택 시 loaded strength 후보가 밀리는 문제와 프로그램 전체 반복 편중 감지 기준을 먼저 테스트 fixture로 남겨야 했다.

수정 골자:
- seed exercise와 canonical metadata를 사용한 v0.4.1.10.4 재현 fixture를 추가했다.
- 4주/5일/45분/60:40/전 장비 선택 조건을 그대로 빌드한다.
- loaded strength 보존 테스트는 현재 v0.4.1.10.3 gap을 재현하므로 관련 fix 커밋에서 활성화하도록 `@Ignore`로 남겼다.
- 반복적 전이 목적 편중을 감지하는 간단한 audit helper를 추가했다.

파일별 변경:
- `ProgramBuilderQualityV041104Test.kt`: 재현 조건 fixture, loaded strength/density 보호 테스트, 반복 편중 audit 테스트.
- `docs/v0.4.1.10.4_program_builder_quality_worklog.md`: v0.4.1.10.4 작업 로그 시작.

동작 변화:
- production 동작 변경 없음.
- loaded strength underuse gap은 테스트에 문서화됐고, 다음 fix 커밋에서 활성화한다.

다음 작업자 참고:
- `badmintonSupportFiveDayFortyFiveMinutePlanKeepsLoadedStrengthAndUsefulDensity`는 all-equipment loaded strength underuse를 직접 겨냥한다.
- `@Ignore`는 Commit 2에서 제거해야 한다.
- 정식 weekly/overall score 비교는 Commit 7 평가 엔진에서 다룬다.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*"` 통과.
 
## Commit 2 - fix(program): preserve loaded strength candidates when all equipment is available

수정 원인:
- The reproduction test showed that all-equipment badminton support plans could still produce no loaded strength items.
- The seed fixture stores equipment tags in Korean tokens such as `바벨`, `덤벨`, `머신`, and `케이블`, while the loaded-strength recognition path only checked English equipment tokens.
- This made normal barbell/dumbbell/machine/cable strength work invisible to the loaded-strength scoring path even when all equipment was selected.

수정 골자:
- Added a loaded-strength flag to `ProgramCandidate`.
- Added a small loaded-strength scoring boost in `ProgramScoringPolicy` when loaded equipment is available.
- Treated both English and Korean seed equipment tokens as loaded strength equipment.
- Enabled the reproduction test that was intentionally ignored in Commit 1.
- This keeps the equipment hard gate intact; it does not allow unselected equipment.

파일별 변경:
- `ProgramCandidate.kt`: identifies loaded strength candidates from exercise equipment and strength-fit metadata.
- `ProgramScoringPolicy.kt`: gives loaded strength candidates a bounded preference so they stay in the selection pool when equipment is available.
- `ProgramBuilderQualityV041104Test.kt`: enables the all-equipment loaded-strength regression and accepts Korean seed equipment tokens.

동작 변화:
- All-equipment selection now preserves ordinary loaded strength candidates instead of letting bodyweight/small-tool/transfer candidates dominate every slot.
- The existing selected-equipment hard gate remains unchanged.
- 6-corner shadow footwork remains allowed.

다음 작업자 참고:
- This commit only fixes loaded-strength recognition and ranking.
- It does not enforce the 60:40 composition quota; that belongs in the next composition-focused commit.
- Do not remove Korean equipment tokens unless the seed/import path is normalized first.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 6 - fix(program): distribute weekly intensity and fatigue across workout days

수정 원인:
- Five-day plans need hard/light rhythm and high-lower/COD fatigue diagnostics without flattening every day into the same intensity.
- Session-level constraints existed, but result-level intensity and consecutive high-stress day diagnostics were missing.

수정 골자:
- Added `ProgramDayIntensityPolicy`.
- Added warnings for missing hard day, missing light day, and consecutive high-lower fatigue clusters.
- Connected the policy to ProgramBuilder after generation.
- Added focused tests for flat-intensity plans and synthetic consecutive high-lower stress.

파일별 변경:
- `ProgramDayIntensityPolicy.kt`: owns hard/light and high-lower fatigue cluster diagnostics.
- `ProgramBuilder.kt`: appends day-intensity warnings after generation.
- `ProgramBuilderQualityV041104Test.kt`: verifies flat intensity and adjacent high-lower cluster detection.

동작 변화:
- Generated plans keep their existing hard/medium/light rhythm.
- The policy warns about repeated high-lower fatigue without forcing all sessions to the same middle intensity.
- No template, equipment, or exercise eligibility hard gate is changed.

다음 작업자 참고:
- Use `ProgramDayIntensityPolicy` for day-level intensity diagnostics.
- Program-wide fatigue scoring belongs in the evaluation engine added next.
- Do not make a single hard week a failure if later weeks recover.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 5 - fix(program): balance weekly exercise and training target distribution

수정 원인:
- Generated plans needed stronger protection against repeated stable keys, repeated movement families, and repeated badminton transfer targets across weeks.
- Candidate collapse is not only a pool-size problem; repeated choices can still dominate after scoring if history penalties and diagnostics are too weak.

수정 골자:
- Strengthened short-gap stableKey, redundancy-group, and movement-family penalties in `ProgramSelectionHistory`.
- Added distribution diagnostics in `ProgramVarietyPolicy`.
- Added ProgramBuilder warnings for weekly stableKey overuse, low weekly distinct exercise count, and program-wide transfer target overuse.
- Added regression coverage for useful weekly distinct exercise count and repeated target detection.

파일별 변경:
- `ProgramSelectionHistory.kt`: increases repeat penalties while preserving anchor repeat allowances.
- `ProgramVarietyPolicy.kt`: owns distribution warnings for weekly and program-wide repetition.
- `ProgramBuilder.kt`: appends distribution warnings after generation.
- `ProgramBuilderQualityV041104Test.kt`: verifies first-week variety and warning detection for synthetic overuse.

동작 변화:
- Same exercise/family/transfer target repetition is less likely to dominate generated plans.
- Anchor exercises can still repeat when useful, but non-anchor repetition is penalized more.
- Single-week imbalance is still not a hard failure; warnings target repeated or program-wide patterns.

다음 작업자 참고:
- `ProgramSelectionHistory` handles selection-time penalties.
- `ProgramVarietyPolicy.distributionWarnings` handles result-level diagnostics.
- Do not reintroduce fixed top-3 pool narrowing; variety needs the wider adaptive pool from v0.4.1.10.3.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 4 - fix(program): require useful minimum density for 45 minute sessions

수정 원인:
- 45-minute sessions should not repeatedly collapse to three or fewer exercises when candidates and time are available.
- The minute-to-exercise-count policy lived inside prescription code, which made density intent harder to test directly.

수정 골자:
- Added `ProgramSessionDensityPolicy`.
- Kept the existing target exercise count bands while exposing useful minimum density separately.
- Added generated-plan warnings when sessions repeatedly fall below the useful minimum.
- Added regression coverage that 30-minute sessions can still use three exercises while 45-minute sessions target useful density.

파일별 변경:
- `ProgramSessionDensityPolicy.kt`: owns target exercise count, useful minimum count, and density warning logic.
- `ProgramPrescriptionPolicy.kt`: delegates exercise count bands to the density policy without changing the current bands.
- `ProgramBuilder.kt`: appends density warnings after generation.
- `ProgramBuilderQualityV041104Test.kt`: verifies the 45-minute reproduction avoids density warnings and the short-session minimum remains lower.

동작 변화:
- 45-minute plans now have an explicit useful-density safeguard.
- Short 20-30 minute plans are still allowed to be around three exercises.
- The optional time-budget trimming behavior remains intact.

다음 작업자 참고:
- Tune session density in `ProgramSessionDensityPolicy`, not by changing unrelated template slots.
- Do not force every 45-minute session to five or more exercises; the useful minimum is four.
- Weekly repetition and target distribution are handled in the next commit.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 3 - fix(program): preserve strength anchors in badminton support plans

수정 원인:
- Loaded-strength candidates were recognized after Commit 2, but badminton support plans still needed an explicit composition preference so transfer work could not crowd out every normal strength anchor.
- The request's 60:40 badminton:strength setting is a composition preference, not a hard template gate.

수정 골자:
- Added `ProgramCompositionPolicy` for badminton-support strength-anchor scoring and underuse warnings.
- Added a bounded composition score adjustment for ANCHOR/SUPPORT candidates without changing hard gates.
- Connected ProgramBuilder to program-level strength-anchor underuse warnings.
- Added ratio-variant tests for 90:10, 60:40, and 40:60 badminton support plans.

파일별 변경:
- `ProgramCompositionPolicy.kt`: owns strength-anchor composition preference and underuse warning logic.
- `ProgramScoringPolicy.kt`: delegates badminton-support strength-anchor score adjustment to the composition policy.
- `ProgramBuilder.kt`: adds composition warnings after generation.
- `ProgramBuilderQualityV041104Test.kt`: verifies strength anchors survive across badminton:strength ratios.

동작 변화:
- Badminton support plans keep normal strength anchors alive even at high badminton-transfer ratios.
- 40:60 strength-biased requests preserve at least as many strength anchors as 60:40.
- This remains a soft composition preference; no selected-equipment, fatigue, template, or session hard gate is relaxed.

다음 작업자 참고:
- Use `ProgramCompositionPolicy` for future ratio/quota tuning.
- Keep exact 60:40 enforcement out of hard gates; minor weekly imbalance should remain repair/evaluation work.
- Session density is still handled in the next commit.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.
