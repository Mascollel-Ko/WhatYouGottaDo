# v0.4.1.10.4 ProgramBuilder Quality Worklog

## Baseline

- Started from `v0.4.1.10.3` on latest `main`.
- Working branch: `fix/program-builder-quality-optimization-v041104`
- Reproduction scenario: badminton support strength plan, 4 weeks, 5 days/week, 45 minutes/day, badminton:strength 60:40, all equipment selected.
- Scope: recommendation quality, internal evaluation, bounded optimization, and progress UI.
- Excluded: 6-corner shadow footwork removal, weekday chip/navigation changes, unselected-equipment hard gate relaxation.

## Commit 1 - test(program): reproduce builder recommendation quality gaps

Commit:
- `83ca2ec`

수정 원인:
- v0.4.1.10.3 split ProgramBuilder structure, but the observed recommendation-quality gaps still needed focused regression coverage.
- The all-equipment 60:40 badminton-support scenario needed to protect loaded strength inclusion, session density, weekly repetition, intensity rhythm, and evaluation recovery behavior.

수정 골자:
- Added a representative quality fixture using seed exercises and canonical metadata.
- Covered loaded strength, strength anchors, 45-minute density, distinct exercise count, intensity rhythm, and synthetic repeated-imbalance evaluation expectations.
- Left the loaded-strength preservation case ignored until the next fix commit because it exposed the baseline gap.

파일별 변경:
- `ProgramBuilderQualityV041104Test.kt`: reproduction and quality regression tests.
- `docs/v0.4.1.10.4_program_builder_quality_worklog.md`: worklog started.

동작 변화:
- No production behavior change.

다음 작업자 참고:
- Keep this fixture as the primary v0.4.1.10.4 quality regression scenario.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*"` passed with the baseline-gap case intentionally ignored.

## Commit 2 - fix(program): preserve loaded strength candidates when all equipment is available

Commit:
- `c77df81`

수정 원인:
- All-equipment plans could still allow loaded barbell/dumbbell/machine/cable strength candidates to be under-ranked by functional transfer candidates.
- This was not an equipment hard-gate problem; it was candidate scoring/composition behavior when all equipment was available.

수정 골자:
- Added loaded-strength candidate detection.
- Added a bounded loaded-strength scoring preference.
- Activated the previously ignored loaded-strength regression test.

파일별 변경:
- `ProgramCandidate.kt`: loaded-strength candidate metadata.
- `ProgramScoringPolicy.kt`: bounded loaded-strength score adjustment.
- `ProgramBuilderQualityV041104Test.kt`: loaded-strength assertions.

동작 변화:
- Loaded strength candidates survive better when all equipment is selected.
- Unselected-equipment restrictions are unchanged.

다음 작업자 참고:
- Do not solve all-equipment underuse by weakening equipment gates.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 3 - fix(program): preserve strength anchors in badminton support plans

Commit:
- `51fc91b`

수정 원인:
- Badminton-support plans still needed an explicit composition preference so transfer work could not crowd out every normal strength anchor.
- The 60:40 ratio is a soft composition intent, not a brittle template hard gate.

수정 골자:
- Added `ProgramCompositionPolicy`.
- Added bounded strength-anchor scoring and underuse warnings.
- Added ratio-variant tests for 90:10, 60:40, and 40:60 badminton-support plans.

파일별 변경:
- `ProgramCompositionPolicy.kt`: strength-anchor composition preference and warnings.
- `ProgramScoringPolicy.kt`: delegates composition score adjustment.
- `ProgramBuilder.kt`: appends composition warnings.
- `ProgramBuilderQualityV041104Test.kt`: ratio coverage.

동작 변화:
- Badminton transfer remains important, but normal strength anchors remain represented.

다음 작업자 참고:
- Future ratio tuning belongs in `ProgramCompositionPolicy`.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 4 - fix(program): require useful minimum density for 45 minute sessions

Commit:
- `0641ef6`

수정 원인:
- 45-minute sessions should not repeatedly collapse to sparse three-exercise sessions when candidates and time are available.
- Density intent was embedded in prescription code and needed a direct policy/testing surface.

수정 골자:
- Added `ProgramSessionDensityPolicy`.
- Kept existing target exercise-count bands while exposing useful minimum density.
- Added density warnings and tests.

파일별 변경:
- `ProgramSessionDensityPolicy.kt`: target and useful minimum exercise counts.
- `ProgramPrescriptionPolicy.kt`: delegates exercise-count bands.
- `ProgramBuilder.kt`: appends density warnings.
- `ProgramBuilderQualityV041104Test.kt`: density regression coverage.

동작 변화:
- 45-minute sessions have an explicit useful-density safeguard.
- 20-30 minute sessions can still be around three exercises.

다음 작업자 참고:
- Do not force 45-minute sessions to five or more exercises by default; useful minimum is four.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 5 - fix(program): balance weekly exercise and training target distribution

Commit:
- `2448a19`

수정 원인:
- Same stableKey, movement family, role, and transfer-purpose repetition could still dominate weekly plans.
- Variety behavior needed stronger selection-time penalties and result-level diagnostics without treating every weekly imbalance as failure.

수정 골자:
- Strengthened `ProgramSelectionHistory` repeat penalties.
- Added `ProgramVarietyPolicy.distributionWarnings`.
- Added regression tests for first-week distinct count and synthetic overuse warnings.

파일별 변경:
- `ProgramSelectionHistory.kt`: repeat penalty tuning.
- `ProgramVarietyPolicy.kt`: distribution warnings.
- `ProgramBuilder.kt`: appends distribution warnings.
- `ProgramBuilderQualityV041104Test.kt`: variety/distribution tests.

동작 변화:
- Repeated exercise/family/transfer collapse is less likely.
- Anchor repetition remains possible when useful.

다음 작업자 참고:
- Do not reintroduce fixed top-3 candidate pools.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 6 - fix(program): distribute weekly intensity and fatigue across workout days

Commit:
- `5d429d8`

수정 원인:
- Five-day weeks needed hard/medium/light rhythm and fatigue clustering diagnostics.
- The builder needed to avoid repeated high-lower neural stress on consecutive workout days without flattening every session.

수정 골자:
- Added `ProgramDayIntensityPolicy`.
- Added flat-intensity and consecutive high-lower warnings.
- Added tests for hard/light presence and fatigue clustering.

파일별 변경:
- `ProgramDayIntensityPolicy.kt`: day intensity classification and warnings.
- `ProgramBuilder.kt`: appends intensity warnings.
- `ProgramBuilderQualityV041104Test.kt`: intensity/fatigue tests.

동작 변화:
- Weekly intensity rhythm is explicitly diagnosed.
- Day-to-day imbalance is allowed; repeated clustering is warned.

다음 작업자 참고:
- Keep hard/medium/light rhythm instead of forcing all sessions into the same middle intensity.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 7 - feat(program): evaluate weekly and overall builder quality

Commit:
- `7efcca1`

수정 원인:
- Recommendation quality needed program-wide evaluation instead of relying on isolated day/week checks.
- The evaluation result should drive repair and diagnostics, not a default UI scorecard.

수정 골자:
- Added `ProgramEvaluationModels`.
- Added `ProgramEvaluationPolicy`.
- Added tests for bad skeleton scoring, recovered imbalance, fatigue, density, strength, transfer, and equipment issues.

파일별 변경:
- `ProgramEvaluationModels.kt`: evaluation scores, issue types, severity, and summaries.
- `ProgramEvaluationPolicy.kt`: weekly/program evaluation.
- `ProgramEvaluationPolicyTest.kt`: evaluation behavior tests.

동작 변화:
- No direct selection behavior change.
- Generated skeletons can now be scored for internal optimization.

다음 작업자 참고:
- Keep evaluation soft enough to allow periodized imbalance that recovers later.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramEvaluation*" --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 8 - feat(program): optimize generated skeleton with evaluation repair loop

Commit:
- `cb6a3e1`

수정 원인:
- Evaluation should feed a bounded repair loop so sparse or imbalanced generated skeletons can be improved automatically.
- The loop must avoid infinite optimization and must not violate equipment/user-exclusion/ratio constraints.

수정 골자:
- Added optimization trace and summary models.
- Added conservative repair policy.
- Added optimization policy with max-iteration and improvement guards.
- Connected `ProgramBuilder` output to optimization.

파일별 변경:
- `ProgramOptimizationTrace.kt`: optimization traces and summary.
- `ProgramRepairPolicy.kt`: conservative repair actions.
- `ProgramOptimizationPolicy.kt`: evaluation/repair/re-evaluation loop.
- `ProgramSkeletonGenerator.kt`: skeleton now carries evaluation and optimization metadata.
- `ProgramBuilder.kt`: optimizes validated skeletons.
- `ProgramOptimizationPolicyTest.kt`: optimization tests.

동작 변화:
- Generated skeletons are internally evaluated and can be improved before return.
- Failed/non-improving repairs leave the prior skeleton intact.

다음 작업자 참고:
- Current repairs are conservative; richer candidate-aware exercise swaps can be added later.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramEvaluation*" --tests "*ProgramOptimization*" --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 9 - feat(program): show progress while optimizing auto skeleton

Commit:
- `ef7ea24`

수정 원인:
- Evaluation and optimization can make auto skeleton generation take several seconds, so the UI must show immediate progress and prevent duplicate actions.

수정 골자:
- Added `ProgramBuildProgressState`.
- Exposed progress state from `TrainingViewModel`.
- Runs skeleton generation on `Dispatchers.Default` with a timeout and staged progress updates.
- Added progress UI, disabled generation/save controls while running, and displayed a short optimization summary.

파일별 변경:
- `ProgramBuildProgressState.kt`: idle/running/completed/failed states.
- `TrainingViewModel.kt`: async generation and progress state.
- `PlanScreen.kt`: progress card, disabled actions, completion/failure UI.
- `PlanEditorControls.kt`: enabled-state support for editor controls.
- `ProgramBuildProgressStateTest.kt`: state model tests.

동작 변화:
- Auto skeleton generation now shows progress and a short adjustment summary.
- Evaluation scorecards are not exposed in the default UI.

다음 작업자 참고:
- Progress percentages are staged, not exact work-unit accounting.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuildProgressStateTest*" --tests "*ProgramBuilderQualityV041104Test*" --tests "*ProgramBuilderCandidateCollapseTest*"` passed.
- `.\gradlew.bat :app:compileDebugKotlin` passed.

## Commit 10 - chore(release): bump version to v0.4.1.10.4

Commit:
- pending until release commit.

수정 원인:
- ProgramBuilder quality optimization work was complete across separate test, policy, evaluation, optimization, and progress UI commits.
- The app version, release notes, and final worklog needed to record the v0.4.1.10.4 behavior and verification path.

수정 골자:
- Bumped `versionName` to `v0.4.1.10.4` and `versionCode` to `401014`.
- Added `docs/v0.4.1.10.4_release_notes.md`.
- Finalized this worklog with the commit-by-commit scope and final validation plan.

파일별 변경:
- `app/build.gradle.kts`: v0.4.1.10.4 app version metadata.
- `docs/v0.4.1.10.4_release_notes.md`: release notes covering baseline, reproduction scenario, fixes, responsibility map, evaluation, optimization, progress UI, risks, and follow-ups.
- `docs/v0.4.1.10.4_program_builder_quality_worklog.md`: release commit entry and final verification record.

동작 변화:
- No runtime behavior change beyond the already committed v0.4.1.10.4 feature commits.

다음 작업자 참고:
- Tune recommendation behavior in the focused policy files, not by re-expanding `ProgramBuilder`.
- Keep candidate trace, required slot relaxation, adaptive pool behavior, optional budget softening, and progress state intact.

검증:
- `.\gradlew.bat --version` passed with Gradle 9.3.0 and Android Studio JBR.
- `.\gradlew.bat :app:testDebugUnitTest` passed.
- `.\gradlew.bat :app:assembleDebug` passed.
