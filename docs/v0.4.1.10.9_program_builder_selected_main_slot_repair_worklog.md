# v0.4.1.10.9 ProgramBuilder Selected Main Slot Repair Worklog

Baseline:
- Started from `main` at `af286a5` (`v0.4.1.10.8`).
- `main` contains `v0.4.1.10.8`: yes.
- Work branch: `fix/program-builder-selected-main-slot-repair-v041109`.

Exact selected-main allowlist:
- `barbell_back_squat`
- `barbell_deadlift`
- `pull_up`
- `ex_32219f7a`
- `ex_8e1b313e`

Exact captain-chair targets:
- `ex_a345e30b`
- `captain_chair_leg_raise`

Broad categories intentionally avoided:
- Squat/hinge/pull/press families, RDLs, rows, lat pulldowns, bench/landmine/leg press variants, other leg raises, all-core penalties, and all-trunk-flexion penalties.

## Commit 1 - test(program): trace selected main candidate collapse

Commit:
- `2551ff3`

수정 원인:
- v0.4.1.10.8 added a 1.40 selected-main score boost and a 0.70 captain-chair penalty, but the generated plan still did not reliably include the exact selected-main exercises.
- That points to selected-main candidates disappearing before final selection, or filler slots being selected before exact main slots are reserved.

수정 골자:
- Added a focused seed-data regression test for the 45-minute badminton support scenario.
- Verified the exact selected-main and captain-chair identities survive hard gates in the reservoir.
- Added assertions for selected-main scoring trace, final selected-main presence, captain-chair repetition, and excluded selected-main hard-gate priority.

파일별 변경:
- `app/src/test/java/com/training/trackplanner/data/ProgramBuilderSelectedMainSlotRepairTest.kt`: adds regression coverage for exact selected-main and captain-chair behavior.
- `docs/v0.4.1.10.9_program_builder_selected_main_slot_repair_worklog.md`: starts the required commit-level worklog.

동작 변화:
- No production behavior change.
- This is test-only diagnostic coverage.

다음 작업자 참고:
- The test intentionally uses exact stable keys only.
- If selected-main exercises are in the reservoir but absent from final items, fix slot reservation/repair, not the hard gate.
- Do not broaden the allowlist to exercise families.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderSelectedMainSlotRepairTest*"`: passed.
- Trace finding: seed-data selected-main targets survive hard gates and reach scoring; `ex_a345e30b` is the seed captain-chair identity, while `captain_chair_leg_raise` remains a legacy score-policy key.
- Trace finding: the seed fixture already selects enough exact selected-main exercises, so the implementation must harden the reported UI path without broadening the selected-main allowlist.

## Commit 2 - fix(program): reserve exact selected main slots before fillers

Commit:
- `5d06224`

수정 원인:
- The existing foundation reservation path could reserve broad slot categories, but it could not force one of the five exact selected-main exercises through a slot.
- Score boosts alone are insufficient when a core/accessory/transfer slot is filled before an exact selected-main candidate is explicitly requested.

수정 골자:
- Added exact selected-main stableKey reservation data to `TemplateExerciseSlot`.
- Taught `ProgramFoundationAnchorPolicy` to reserve exact selected-main slots for 40+ minute badminton support sessions when hard-gate-eligible selected-main candidates are available.
- Taught `ProgramSlotCandidateQuery` to only admit the exact reserved stableKey for an exact selected-main slot.
- Kept the v0.4.1.10.8 1.40 selected-main boost and 0.70 captain-chair penalty.

파일별 변경:
- `ProgramSelectedExerciseScorePolicy.kt`: exposes exact selected-main and captain-chair identity helpers.
- `ProgramTemplateCatalog.kt`: adds `selectedMainStableKey` to template exercise slots.
- `ProgramFoundationAnchorPolicy.kt`: reserves exact selected-main slots before filler slots, without broadening the allowlist.
- `ProgramSlotCandidateQuery.kt`: filters exact selected-main slots by stableKey before scoring.
- `ProgramSkeletonGenerator.kt`: adds selected-main reservation key to candidate trace.
- `ProgramBuilder.kt`: passes hard-gate-eligible selected-main stable keys into the reservation policy.
- `ProgramBuilderSelectedMainSlotRepairTest.kt`: adds exact reservation and broad-family rejection checks.

동작 변화:
- 40+ minute badminton support generation can now reserve exact selected-main exercises before core/accessory/transfer filler slots.
- Broad family substitutes such as goblet squat do not satisfy an exact `barbell_back_squat` reservation.
- Excluded or equipment-filtered selected-main targets are not forced back into the plan because reservation only sees reservoir candidates.

다음 작업자 참고:
- `selectedMainStableKey` is intentionally exact identity data, not a movement-family hint.
- Do not replace this with squat/hinge/pull/press family matching.
- Captain-chair conditional blocking is still separate and follows in the next commit.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderSelectedMainSlotRepairTest*"`: passed.
- `.\gradlew.bat :app:compileDebugKotlin`: passed.

## Commit 3 - fix(program): block captain chair until selected main requirements are met

Commit:
- `2e021ae`

수정 원인:
- Captain chair leg raise was only penalized in scoring, so it could still be considered as a filler before the selected-main weekly/program minimums were satisfied.
- The requested behavior is not a blanket core or leg-raise block; it is an exact captain-chair conditional block.

수정 골자:
- Added stableKey-level selected-main and captain-chair helpers.
- Added a `captainChairAllowed` query gate with default pass-through behavior.
- ProgramBuilder now blocks exact captain-chair candidates while selected-main requirements remain open, and after one program-wide captain-chair use.
- Added trace fields for captain-chair block count and reason.

파일별 변경:
- `ProgramSelectedExerciseScorePolicy.kt`: adds exact stableKey helper methods.
- `ProgramSlotCandidateQuery.kt`: adds conditional captain-chair gate and trace warning/count.
- `ProgramSkeletonGenerator.kt`: adds captain-chair block trace fields.
- `ProgramBuilder.kt`: computes selected-main weekly/program readiness before allowing captain-chair candidates.
- `ProgramBuilderSelectedMainSlotRepairTest.kt`: covers query-level captain-chair blocking.

동작 변화:
- Captain chair remains allowed only after exact selected-main requirements are satisfied and only as a low-priority filler up to one program-wide use.
- Other core, leg raise, and trunk-flexion exercises are not blocked by this policy.

다음 작업자 참고:
- Keep this block exact to `ex_a345e30b` / `captain_chair_leg_raise`.
- Do not move broad core-pattern policy into this gate.
- Post-generation repair still needs to replace already-filled weak slots when selected-main is missing.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderSelectedMainSlotRepairTest*"`: passed.
- `.\gradlew.bat :app:compileDebugKotlin`: passed.

## Commit 4 - fix(program): repair filler slots with exact selected main exercises

Commit:
- `4ec7961`

수정 원인:
- Even with slot reservation, an already-generated skeleton can still be missing exact selected-main exercises if weak filler slots were selected earlier or if optimization receives a degraded intermediate result.
- The existing repair path replaced weak slots with broad foundation tiers, not the exact selected-main allowlist.

수정 골자:
- Added `SELECTED_MAIN_MISSING` as an explicit evaluation issue.
- Program evaluation now detects when selected-main candidates were available in trace but no exact selected-main item exists in the result.
- Issue-driven repair now handles `SELECTED_MAIN_MISSING` before broad foundation repair.
- Repair reopens captain-chair/core/filler/weak slots and inserts an exact selected-main candidate from the reservoir.

파일별 변경:
- `ProgramEvaluationModels.kt`: adds `SELECTED_MAIN_MISSING`.
- `ProgramEvaluationPolicy.kt`: emits the selected-main missing issue from trace-backed availability.
- `ProgramIssueDrivenRerankPolicy.kt`: replaces weak filler slots with exact selected-main reservoir candidates.
- `ProgramOptimizationPolicy.kt`: adds a user-facing summary message for selected-main repair.
- `ProgramBuilderSelectedMainSlotRepairTest.kt`: covers exact selected-main repair from a captain-chair filler slot.

동작 변화:
- If exact selected-main candidates were available but missing, optimization can now replace a weak filler slot with one of the exact allowed exercises.
- Repair still uses only reservoir candidates, so unavailable equipment, excluded exercises, and direct hard gates are not bypassed.

다음 작업자 참고:
- This repair is exact selected-main repair. It must not be changed into broad squat/hinge/pull/press repair.
- Evaluation cap for unrepaired selected-main absence follows in the next commit.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderSelectedMainSlotRepairTest*"`: passed.
- `.\gradlew.bat :app:compileDebugKotlin`: passed.

## Commit 5 - fix(program): cap quality scores when selected main exercises are missing

Commit:
- pending

수정 원인:
- A 45-minute badminton support plan with available exact selected-main candidates but zero selected-main results must not receive a high or perfect quality score.
- Repair should try first, but if a selected-main-missing skeleton remains, evaluation must make the failure visible.

수정 골자:
- Added a strong overall score cap for `SELECTED_MAIN_MISSING`.
- Added a regression test proving trace-backed selected-main availability with zero selected-main items caps the score at 60 or below.

파일별 변경:
- `ProgramEvaluationPolicy.kt`: caps `SELECTED_MAIN_MISSING` evaluations.
- `ProgramBuilderSelectedMainSlotRepairTest.kt`: adds selected-main missing cap coverage.

동작 변화:
- Selected-main availability with zero selected-main results can no longer evaluate as a high-score or 100-point program.
- The cap remains exact to selected-main missing and does not broaden penalties to all core or leg-raise patterns.

다음 작업자 참고:
- The cap is intentionally narrow and should stay coupled to exact selected-main availability/missing status.
- Trace expansion for cap reason follows in the next commit.

검증:
- `.\gradlew.bat :app:testDebugUnitTest --tests "*ProgramBuilderSelectedMainSlotRepairTest*"`: passed.
- `.\gradlew.bat :app:compileDebugKotlin`: passed.
