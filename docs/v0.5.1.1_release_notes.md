# WhatYouGottaDo 0.5.1.1 Exercise Identity and Image Reconciliation

## Baseline

- Started from `origin/main` commit `46490c11bf7cac0f009711ede6962b37406549eb`.
- Release identity: `0.5.1.1 / 501001`.

## Changes

- Reconciled the canonical inventory to 253 identities: 242 selectable and 11
  history-only.
- Completed one-to-one stable-key image coverage for all 253 identities and
  normalized the mapped images to 384 x 384.
- Added the general inverted row as a selectable BODYWEIGHT exercise.
- Preserved the historical one-arm inverted-row identity and its image without
  inferring a ring or suspension variant.
- Applied the pull-up core interpretation to weighted pull-up, inverted row,
  and historical one-arm inverted row (`HIDDEN_MODERATE`). Reviewed one-arm
  ring and suspension rows remain `HIDDEN_HIGH`.
- Retired the approved superseded generic hip adduction, pull-up, one-leg RDL,
  reverse curl, and preacher curl identities. Program references migrate to
  their approved concrete replacements while workout history remains intact.
- Regenerated metadata, localization, tissue-prior, program parity, and
  contract artifacts from their canonical sources.

## Preserved Behavior

- ProgramBuilder week count, selected weekdays, daily time, and
  badminton-to-strength ratio behavior are unchanged. All 307 golden rows
  matched after applying only the approved stable-key substitutions.
- Historical workout records remain available to analysis.
- Existing ring and suspension equipment identities retain their reviewed
  semantics.

## Verification

- Metadata authority tests: 9 passed.
- Localization tests: 10 passed.
- Focused image, identity, core, backup, tissue, and ProgramBuilder tests passed.
- `:app:testDebugUnitTest`: 1,229 passed, 0 failed.
- `:app:assembleDebug`: passed.
- `git diff --check`: passed.
- Debug APK: `app/build/outputs/apk/debug/app-debug.apk` (64,933,567 bytes).
- `aapt` verified version name `0.5.1.1` and version code `501001`.

## Commit

- `c4cbaee feat(metadata): reconcile exercise identities and image assets`
