# v0.3.4.4.2 Precheck Report

## Repository State

- Branch: `main`
- Git status before edits: clean and tracking `origin/main`
- Latest local commit: `b108747 v0.3.4.4.1: structure initial profile inputs`
- Existing tags checked: `v0.3.4.4`, `v0.3.4.4.1`
- Current `versionCode`: `304041`
- Current `versionName`: `0.3.4.4.1`
- Current Room DB version: `10`
- Current schema exports: `9.json`, `10.json`

## Current Implementation Table

| 항목 | 현재 상태 | 문제 여부 | v0.3.4.4.2에서 처리 방식 |
| --- | --- | --- | --- |
| InitialProfileDialog | v0.3.4.4.1에서 numeric/chip 기반 UI가 대부분 들어감. `freeNote`만 자유 메모로 남음. | 부분 보완 필요 | 저장값 검증 유지. 핵심 분석 입력은 structured key/numeric만 사용. |
| InitialUserProfile schema | v10에 구조화 필드가 추가됨. RPE는 `Double?`/REAL로 저장됨. | 큰 문제 없음 | 파괴적 타입 변경 없이 v11 no-op migration. RPE는 UI에서 1~10 선택값만 저장. |
| DB migration | `8->9`, `9->10` 존재. `9->10`이 구조화 필드를 추가함. | v0.3.4.4.2 버전용 migration 없음 | DB version 11 추가, `10->11` no-op migration 추가. 기존 데이터 보존. |
| Initial adaptation model | 별도 모델 없음. | 문제 있음 | `InitialAdaptationProfile` 및 calculator 추가. |
| Readiness engine binding | `InitialProfileReadinessAdjuster`가 `FATIGUED`일 때만 개입함. | 문제 있음 | cold-start baseline을 pressure 계산 전에 반영하고, 최종 summary 보정도 모든 상태에서 가능하게 수정. |
| Backup/restore | 구조화 profile key export/import는 일부 구현됨. legacy free text fallback 존재. | 부분 보완 필요 | 새 버전 보고서에 호환 정책 명시. 필요한 key 유지. |
| Tests | readiness, CSV, migration 일부 테스트 존재. cold-start profile 영향 테스트 없음. | 문제 있음 | adaptation/profile binding 단위 테스트 추가. |

## Key Findings

1. v0.3.4.4.1 is present as a committed baseline, not a dirty partial working tree.
2. The profile UI and DB fields were mostly structured in v0.3.4.4.1.
3. The actual readiness binding is still incomplete because `InitialProfileReadinessAdjuster` returns immediately unless the status is `FATIGUED`.
4. No separate cold-start adaptation/baseline model exists yet.
5. The existing status enum remains `READY`, `CAUTION`, `FATIGUED`, `LIMITED`; v0.3.4.4.2 will not replace the status system.
6. v0.3.4.4.2 must focus on completing the baseline binding and tests, not broad UI redesign.
