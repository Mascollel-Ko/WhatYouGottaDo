# v0.3.4.4.3 Test Report

## UI Follow-up Addendum

Additional checks for the profile/record/program UI follow-up:

- `git diff --check`: passed with CRLF warnings only.
- Targeted `testDebugUnitTest` suite: passed.
- Covered tests:
  - `FatiguePressureCalculatorHotfixTest`
  - `PerformanceDropDetectorHotfixTest`
  - `InitialProfileColdStartReadinessTest`
  - `RecordCsvBackupRestoreTest`

The Gradle wrapper initially required network access to fetch/use the configured Gradle distribution. After running the same minimum test command with network access, the build and targeted tests completed successfully.

APK build is tracked outside this committed test report because the latest instruction requires commit/push after tests and before APK build.

## Targeted Tests

Executed after the focused code changes:

```text
.\gradlew.bat --no-daemon --no-problems-report testDebugUnitTest --tests "com.training.trackplanner.analysis.readiness.FatiguePressureCalculatorHotfixTest" --tests "com.training.trackplanner.analysis.readiness.PerformanceDropDetectorHotfixTest" --tests "com.training.trackplanner.analysis.readiness.InitialProfileColdStartReadinessTest" --tests "com.training.trackplanner.data.RecordCsvBackupRestoreTest"
```

Result: passed.

## Direct Recheck

After removing stale e1RM reason code:

```text
.\gradlew.bat --no-daemon --no-problems-report testDebugUnitTest --tests "com.training.trackplanner.analysis.readiness.PerformanceDropDetectorHotfixTest"
```

Result: passed.

## Android Test Compilation

```text
.\gradlew.bat --no-daemon --no-problems-report assembleDebugAndroidTest
```

Result: passed. This compile-checks the new Room migration test and exports schema `12.json`.

## Not Run

`connectedDebugAndroidTest` was not run because no device/emulator execution was requested or available in this workflow. The migration test was added and compiled successfully.

## Final Build

Final `assembleDebug` did not complete in this environment.

Attempts:

```text
.\gradlew.bat --no-daemon --no-problems-report assembleDebug
```

Result: failed before build execution because the sandbox blocked the Gradle wrapper distribution download.

```text
C:\Users\pki08\.gradle\wrapper\dists\gradle-9.3.0-bin\...\gradle.bat --offline --no-daemon --no-problems-report assembleDebug
```

Result: failed during settings plugin resolution for `org.gradle.toolchains.foojay-resolver-convention`.

```text
work\tools\gradle-8.10.2\bin\gradle.bat --offline --no-daemon assembleDebug
```

Result: failed for the same settings plugin resolution issue.

No v0.3.4.4.3 APK was produced. The existing `app-debug.apk` metadata still reports `v0.3.4.4.2`, so it was not copied to the backup drive.
