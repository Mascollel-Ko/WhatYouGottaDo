# v0.3.5.0 Next Patch Recommendations

## v0.3.5.0 - Candidate Generation

- Build a report-only seed patch candidate generator.
- Normalize all production placeholders and blank defaults.
- Translate final sidecar fields to current app taxonomy explicitly.
- Emit candidate files and a 215-row diff; do not mutate assets.
- Preserve all current draft/audit artifacts.

## v0.3.5.1 - Metadata Integration / Recommendation Mapping

- Decide which new fields map to existing Exercise columns and which require Room migration.
- Add candidate-to-Exercise loader/repository mapping.
- Add one stableKey-first resolver and use it in seed upsert, restore, and imports.
- Add exercise_stable_key to backup set rows; keep name fallback for older backups.
- Map movementFamily/subtype, programSlot, redundancyGroup, stress profiles and badminton transfer taxonomy.
- Update ProgramSkeletonGenerator to use programSlot/redundancyGroup instead of movementPattern-only suppression.
- Keep name parsing only for search and legacy/custom import fallback.
- Add migration and focused integration tests only when implementation begins.

## v0.3.6.0 - Analysis Graphs

- Extend existing metadata-based analysis rather than replacing it.
- Add detailed muscle-volume distribution using normalized tags/contributions.
- Add tendon, joint-stability and impact stress aggregation.
- Use primaryStressProfile, secondary tags, recovery decay and stress magnitude in fatigue scoring.
- Add badminton transfer level/type/skill/physical-quality scoring.
- Add charts and interpretation panels with source-confidence gating.

## Final Answers

1. Classification complete enough for seed candidate generation: **YES**.
2. Immediate seed mutation allowed: **NO**.
3. Separate seed candidate generator needed: **YES**.
4. Current app uses structured metadata in analysis: **YES**, but not the final v0.3.5.0 sidecar taxonomy.
5. Main mapping gap: new sidecar fields are not normalized into Exercise; records/backups do not carry stableKey at record level.
6. v0.3.5.1: normalization adapter, schema decision, stableKey-first resolver, backup identity, recommendation/program mapping.
7. v0.3.6.0: detailed metadata-based volume, stress, fatigue, badminton scoring and graphs.
