# v0.3.5.3 ProgramBuilder Architecture Report

## Result

- Version: `versionCode 305003`, `versionName v0.3.5.3`.
- `ProgramBuilder.kt`: reduced from 916 to 737 lines.
- Full hidden-template feature: not implemented.
- Generated-program formulas, existing day-count templates, week waves, scoring, prescription, and guardrails: preserved.

## Added Components

- `ProgramTemplateCatalog.kt`: current slot skeleton, week flow, and role templates.
- `ProgramSlotDefinition.kt`: centralized future slot identity and category flags.
- `SlotCapabilityResolver.kt`: canonical metadata-first slot capability resolution.
- `ExposureTargetTable.kt`: qualitative exposure target foundation.
- `FatigueSlotPolicy.kt`: extracted current fatigue gate and day-intensity adaptation.
- `CoverageAccountingPolicy.kt`: full/partial/weak coverage credit and 215-row diagnostic.

## Modified Integration

- `ProgramBuilder.kt` now orchestrates the extracted components.
- `ProgramSkeletonItem` carries primary, secondary, weak, and source capability fields.
- Two-week movement validation consumes capability credits first and preserves a legacy fallback for capability-free historical/test objects.
- Canonical metadata is resolved once per candidate instead of twice.

## Verification

Focused ProgramBuilder suite:

```text
:app:testDebugUnitTest
ProgramArchitectureFoundationTest
ProgramBuilderCanonicalExamplesTest
ProgramBuilderV0352Test
ProgramBuilderRehabActivationTest
ProgramSkeletonGeneratorTest
Result: PASS, 37 tests
```

Adjacent metadata suite:

```text
ExerciseMetadataAdapterTest
MetadataSanityCheckerTest
RuntimeExerciseMetadataAssetLoaderTest
RuntimeExerciseMetadataResolverTest
RuntimeExerciseMetadataRoundTripTest
ProgramGeneratorUsesResolvedRuntimeMetadataTest
Result: PASS
```

Build:

```text
:app:assembleDebug
Result: BUILD SUCCESSFUL
APK: app/build/outputs/apk/debug/app-debug.apk
Size: 42,360,027 bytes
SHA-256: 5B91F329C82B588FA660DAC6AD5A5EEDA8920D46C258BB43E2B233328FAFA0FF
```

Backup:

```text
C:/Users/pki08/Documents/Codex/google_drive_backup/v0.3.5.3-WhatYouGottaDo-debug.apk
Size and SHA-256 match the build output.
```

## Safety Checks

- `training_settings_seed.csv`: unchanged; SHA-256 `9F2CB1240E1C0D822522451CA9F47F03EF3FF6DD11CF8B13385BC7D85B4F802F`.
- `exercises_seed.json`: unchanged; SHA-256 `3B1A16210C8334B0C98DB0F18B4F7DAD606A3A08661FC19125641ED3044056E4`.
- Room schema: unchanged at version 14.
- Canonical metadata CSV: unchanged.
- UI and user DB: unchanged.

## Remaining Work

- Implement the full hidden-template catalog in a later patch.
- Tune low-coverage future slots only after metadata review.
- Introduce slot-specific fatigue substitutions without changing the existing global guardrails.
