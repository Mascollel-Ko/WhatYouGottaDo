# previous cold-start baseline release Test Report

## Targeted Tests

The following tests were run first because they directly cover the changed code paths:

```powershell
.\gradlew.bat --no-daemon --no-problems-report testDebugUnitTest --tests "com.training.trackplanner.analysis.readiness.InitialProfileColdStartReadinessTest"
```

Result: success after fixing the cold-start high-profile downgrade condition to use pressure ratio rather than percentile-only labels.

```powershell
.\gradlew.bat --no-daemon --no-problems-report testDebugUnitTest --tests "com.training.trackplanner.data.RecordCsvBackupRestoreTest"
```

Result: success.

## Final Checks

```powershell
.\gradlew.bat --no-daemon --no-problems-report assembleDebugAndroidTest
```

Result: success. This generated `app/schemas/com.training.trackplanner.data.TrainingDatabase/11.json`.

```powershell
.\gradlew.bat --no-daemon --no-problems-report testDebugUnitTest
```

Result: success.

```powershell
.\gradlew.bat --no-daemon --no-problems-report assembleDebug
```

Result: success.

## APK Backup

- Copied APK: `C:\Users\pki08\Documents\Codex\google_drive_backup\archived-previous-cold-start-baseline-debug.apk`
- File size: `41977107` bytes
